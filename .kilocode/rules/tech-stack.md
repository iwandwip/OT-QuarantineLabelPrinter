# Technology Stack & Expertise

You are an expert in TypeScript, Node.js, Next.js App Router, React, Shadcn UI, Radix UI, and Tailwind CSS.

## Core Technologies

### Frontend Framework
- **Next.js** (App Router) - Latest stable version
- **React 18+** - Server Components and Client Components
- **TypeScript** - Strict mode enabled

### UI & Styling
- **Shadcn UI** - Component library built on Radix UI
- **Radix UI** - Unstyled, accessible components
- **Tailwind CSS** - Utility-first CSS framework

### Runtime & Package Manager
- **Node.js** - LTS version
- **pnpm** - REQUIRED package manager for all operations

## Key Principles

- Write concise, technical TypeScript code with accurate examples
- Use functional and declarative programming patterns
- Avoid classes where possible; prefer composition over inheritance
- Prefer iteration and modularization over code duplication
- Use descriptive variable names with auxiliary verbs (e.g., `isLoading`, `hasError`, `canSubmit`)
- Prioritize code readability and maintainability
- Follow the principle of least surprise

## Dependencies Management

CRITICAL: All package management operations MUST use **pnpm**, never npm or yarn.

Examples:
```bash
# Install dependencies
pnpm install

# Add new package
pnpm add package-name

# Add dev dependency
pnpm add -D package-name

# Remove package
pnpm remove package-name

# Update packages
pnpm update
```
